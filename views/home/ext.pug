extends base

include ./mixins/home
include ./mixins/mail
include ./mixins/cardPopup
include ./mixins/harry

block content
  .iosOverflow
    +cardPopup()
    +home(cards, activeFilters)
    +mail(subscribePage)
    +harry()